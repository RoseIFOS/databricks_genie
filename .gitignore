# Segredos / credenciais — NUNCA versionar
.env
.env.*
*.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / frontend
node_modules/
dist/
.vite/

# Databricks
.databricks/

# Power BI (PBIP) — artefatos regeneraveis / por-maquina (nao sao fonte de verdade)
**/.pbi/cache.abf
**/.pbi/localSettings.json

# Notas pessoais em PDF (contêm prints de credenciais) — nunca versionar.
# Mantemos docs/manual_azure_novo/ (markdown) versionado.
docs/manual_azure_antigo/

# Material do mini-curso — PRODUTO da autora, não vai no repo (conhecimento pago).
# Docs operacionais (RUNBOOK_*, PLANO, READMEs) continuam versionados normalmente.
docs/curso/

# Conteúdo de marketing (posts/artigos) — ativo da autora, mantido local até publicar.
docs/posts/
