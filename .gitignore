# Segredos / credenciais — NUNCA versionar
.env
.env.*
*.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / frontend
node_modules/
dist/
.vite/

# Databricks
.databricks/

# Power BI (PBIP) — artefatos regeneraveis / por-maquina (nao sao fonte de verdade)
**/.pbi/cache.abf
**/.pbi/localSettings.json

# Toda a pasta docs/ é LOCAL — não versionar no repo técnico:
#   - manual_azure_novo (estudo) · curso (produto) · posts (marketing)
#   - "HPN - Global Report.*" (o Power BI de referência)
# Removida do tracking em 2026-07-16 (git rm --cached; arquivos mantidos no disco).
# Docs operacionais (RUNBOOK_*.md, PLANO_*, READMEs na raiz) seguem versionados.
docs/
