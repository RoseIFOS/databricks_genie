# Dependências do backend FastAPI (frontend principal React).
# A versão Streamlit alternativa tem seu próprio requirements em alt_streamlit/.
fastapi>=0.115
uvicorn[standard]>=0.30
databricks-sdk>=0.36
pandas>=2.2
psycopg[binary]>=3.2
